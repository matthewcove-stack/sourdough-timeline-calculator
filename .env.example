APP_HOST=0.0.0.0
APP_PORT=4173
BASE_URL=http://sourdough.localhost
PROXY_BASE_PATH=/
TRUST_PROXY=true
VITE_BASE_PATH=/
EDGE_NETWORK=edge

# Optional direct LAN exposure override:
# docker compose -f compose.yml -f compose.lan.yml up -d --build
LAN_BIND_ADDR=0.0.0.0
HOST_PORT=4173
